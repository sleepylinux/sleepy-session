# Task 3 Fix R3 Static Store Symlink Resolution Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Resolve complete approved Home Manager Niri symlink chains inside `/nix/store` without pathname races or policy escape.

**Architecture:** Keep writable and ordinary source-entry handling unchanged. Replace only the static-store leaf reader with a private descriptor-relative resolver that normalizes each absolute or relative link target against a fixed store root, rejects lexical escape, detects loops, bounds hops, and reads the root-owned final regular file from the descriptor it fstats. Production fixes the policy to `/nix/store` and uid 0; unit tests inject a temporary store root and its owner without exposing a weaker production API.

**Tech Stack:** Rust 2021, libc `openat`/`fstat`/`readlinkat`, `OwnedFd`, `HashSet`, Cargo tests.

**Spec:** Task 3 Fix R3 payload received 2026-08-24, extending `task-3-brief.md` and Fix R1/R2.

## Global Constraints

- Strict RED before production changes; one separate fix commit; no push.
- Initial static source link and every later hop must remain lexically and descriptor-safely inside `/nix/store`.
- Writable paths and normal regular source entries retain `O_NOFOLLOW` behavior.
- Reject escape, loops, excessive hops, symlinked escape components, and non-regular finals.

---

### Task 1: Closed static-store chain resolver

**Files:**
- Modify: `src/store/secure_fs.rs`
- Test: `src/store/secure_fs.rs`
- Verify existing TOCTOU test: `tests/bindings.rs`

**Interfaces:**
- Consumes: initial link text returned by `SecureDir::open_entry`.
- Produces: `SecureDir::read_root_store_regular`, still the only production entry and still fixed to `/nix/store` plus uid 0.

- [ ] **Step 1: Add unit RED tests**

Add private-module tests for an absolute home target followed by a store symlink to a regular file, a relative multi-hop chain, an outside-store absolute/`..` escape, a loop, and a non-regular final. Use a temporary store root and the current uid only through the private policy helper.

- [ ] **Step 2: Verify RED**

Run the focused secure-fs tests. Expected: valid chain tests fail with `unsafe_path` because the current walk refuses store-side symlinks.

- [ ] **Step 3: Implement minimal resolver**

Open the configured store root through no-follow directory descriptors. Normalize the initial absolute path into store-relative components. At each component, `openat(O_NOFOLLOW)` and `fstat` the returned fd; on a symlink use `readlinkat` on its fd, lexically resolve against its parent, append unvisited suffix components, reject escape, record the normalized state for loop detection, and restart from the retained store-root fd. Enforce 40 hops. Read only a final regular file owned by the configured uid from the same descriptor.

- [ ] **Step 4: Verify GREEN and regression scope**

Run the focused chain tests, the existing source-entry swap test, malicious-link tests, and full bindings suite. Confirm writable and ordinary source-entry code is unchanged.

- [ ] **Step 5: Full verification, report, and commit**

Run fmt check, clippy all targets/features with warnings denied, debug/release all-target tests, release all-target build, and diff checks. Append root cause and exact RED/GREEN evidence to the Task 3 report, commit `fix: resolve approved niri store symlink chains`, keep the branch clean, and do not push.
