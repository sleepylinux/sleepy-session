# Task 3 Fix R2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make binding initialization a true idempotent initializer, close journal publication crash gaps, eliminate Niri source-tree pathname races, and bound every production Niri subprocess.

**Architecture:** Keep the existing one-lock binding transaction and retained `SecureDir` roots. Add closed descriptor APIs for safe source traversal and atomic create-new publication, and place all subprocess lifecycle/deadline behavior behind production validator/reloader helpers with injected tests plus real fake executables.

**Tech Stack:** Rust 2021, libc `openat`/`renameat`/`fsync`/`readlinkat`, serde JSON, std process/thread/channel APIs, Cargo integration tests.

**Spec:** Task 3 Fix R2 payload received 2026-08-24, extending `task-3-brief.md` and Fix R1.

## Global Constraints

- Strict RED before production changes and separate commits per review slice.
- No shell-based tests or production shell snippets.
- No push, destructive cleanup, or user state loss.
- Writable state stays descriptor-relative and journal cleanup never removes unrelated files.
- Niri initial snapshot is the pinned >=26.04 `ConfigLoaded` then terminal `CastsChanged` protocol.

---

### Task 1: Idempotent initializer after confirmed reconciliation

**Files:**
- Modify: `src/bindings/apply.rs`
- Modify: `tests/bindings.rs`
- Modify: `tests/cli.rs`

**Interfaces:**
- Consumes: `initialize_bindings`, `reconcile_bindings`, retained `BindingFileSystem`.
- Produces: initializer no-op report when settings, presets, and generated include already form the compiled active state.

- [ ] **Step 1: Write failing library and CLI tests**

Create an offline initialization, reconcile it online so the journal is cleared, record exact bytes and metadata modification times, then call initialize offline and assert no journal, no reload request, exact bytes, and exact mtimes.

- [ ] **Step 2: Verify RED**

Run the two focused tests. Expected failure: second initialize publishes a new `reloadPending` journal and changes artifact metadata.

- [ ] **Step 3: Implement minimal coherent-state no-op**

Under the existing store lock, load/validate settings and presets, compile the active preset, descriptor-read the generated include, and return `Committed` without validator/reloader/journal activity only when the bytes equal compiled output. Missing include continues through normal initialization.

- [ ] **Step 4: Verify GREEN and commit**

Run focused tests and all binding/CLI tests, then commit `fix: make binding initialization idempotent`.

### Task 2: Crash-safe initial journal and sidecar publication

**Files:**
- Modify: `src/store/secure_fs.rs`
- Modify: `src/bindings/journal.rs`
- Modify: `src/bindings/apply.rs`
- Modify: `tests/bindings.rs`

**Interfaces:**
- Consumes: retained journal/artifact `SecureDir` descriptors and `ApplyObserver`.
- Produces: create-new atomic publication with explicit partial-write, file-sync, rename, and directory-sync fault seams.

- [ ] **Step 1: Write failing publication fault tests**

Inject failures during partial temp write, before/inside temp file sync, after rename, and during directory sync for the initial journal and every sidecar. Include truncated temp/final fixtures and ENOSPC-like write failures; restart must remove only the transaction UUID files and preserve unrelated files.

- [ ] **Step 2: Verify RED**

Expected failures: no inner write/sync stages exist, truncated final journal produces `invalid_journal`, and incomplete sidecar validation rejects cleanup before it can recover.

- [ ] **Step 3: Implement atomic publication and incomplete cleanup**

Add descriptor-relative create-new temp write/sync/rename/fsync-dir primitive. Publish the initial journal only after complete temp sync. Atomically publish sidecars. When `sidecarsComplete=false`, validate only the closed header/path ownership and delete exact UUID sidecars and temp names without requiring their hashes/content.

- [ ] **Step 4: Verify GREEN and commit**

Run publication/restart fault matrices and binding suite, then commit `fix: atomically publish binding transaction artifacts`.

### Task 3: Exact descriptor-relative Niri source traversal

**Files:**
- Modify: `src/store/secure_fs.rs`
- Modify: `src/bindings/apply.rs`
- Modify: `tests/bindings.rs`

**Interfaces:**
- Consumes: retained Niri root fd.
- Produces: entry handles opened with `openat(O_NOFOLLOW)` and classified/read recursively from the same fd; separately validated `/nix/store` regular targets for static symlinks.

- [ ] **Step 1: Write failing root and recursive swap tests**

At the production enumeration/open seam, replace a regular file with a symlink to attacker bytes at root and in a nested directory. Assert validation rejects or copies the originally opened regular file, never attacker bytes.

- [ ] **Step 2: Verify RED**

Expected failure: current `/proc/<pid>/fd/...` pathname metadata/read traversal follows the swapped symlink.

- [ ] **Step 3: Implement exact-fd traversal**

Enumerate retained directories, open each entry relative to that fd with `O_NOFOLLOW`, fstat and read/recurse through that same descriptor. Resolve static symlink text with `readlinkat`, require an absolute `/nix/store` target, open it `O_NOFOLLOW`, and require root-owned regular metadata on the opened fd.

- [ ] **Step 4: Verify GREEN and commit**

Run swap, static-link, validation, and complete binding suites, then commit `fix: traverse niri sources through retained descriptors`.

### Task 4: Deadline and reap every production Niri subprocess

**Files:**
- Modify: `src/bindings/apply.rs`
- Modify: `src/cli.rs`
- Modify: `tests/cli.rs`

**Interfaces:**
- Consumes: fixed total online deadline and existing validator/reloader interfaces.
- Produces: bounded version, event subscription/barrier, validation, candidate load, and rollback load calls with kill+wait on timeout and typed errors.

- [ ] **Step 1: Write failing real-process tests**

Build one Rust fake executable mode per phase without shell. Each mode records its PID, hangs, and is invoked through the real `NiriValidator`/`NiriReloader`/CLI. Assert bounded wall time, expected `validation_failed`, `niri_unavailable`, or honest reload status, and `/proc/<pid>` disappearance after return.

- [ ] **Step 2: Verify RED**

Run each phase test under an outer test timeout. Expected failure: version, validator, or load child remains blocked/alive beyond the contract.

- [ ] **Step 3: Implement deadline-aware child runner**

Use a monotonic deadline shared by online-required reconciliation. Poll child completion until the deadline; on expiry call `kill`, then `wait` before returning. Event-stream ownership likewise kills and waits on timeout/drop. Route validator, version query, explicit candidate load, and rollback load through the bounded runner.

- [ ] **Step 4: Verify GREEN and commit**

Run all process modes, full debug/release tests, fmt, clippy all targets/features with warnings denied, release build, and diff checks. Commit `fix: bound and reap niri subprocesses`.

### Task 5: Report and final audit

**Files:**
- Modify: `sleepy/.superpowers/sdd/2026-08-24-desktop-m2-control-center/task-3-report.md`

**Interfaces:**
- Consumes: exact RED/GREEN output and commit SHAs.
- Produces: auditable Fix R2 root-cause, test, verification, and limitation record.

- [ ] **Step 1: Append exact evidence and limitations**

Record each root cause, observed RED, GREEN command/result, commit, real-Niri/Nix availability, and no-push status.

- [ ] **Step 2: Run final verification**

Run fmt check, clippy all targets/features with `-D warnings`, debug and release all-target tests, release all-target build, `git diff --check`, commit-range review, and clean status.

- [ ] **Step 3: Return final SHA**

Send the parent the clean branch status, commit list, verification counts, report path, and honest environment limitations.
